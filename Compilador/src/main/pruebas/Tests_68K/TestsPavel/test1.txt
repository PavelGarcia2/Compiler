/* FALLOS : No entra en el bucle a pesar que deberia,
 no se generan bien las etiquetas ya que se muestran como literales , y 
 	JMP fsalto0; Instruction of type: SKIP pne el punto y coma pegado y peta
   NO_SOLUCIONADO
*/
int i =0;
main {
    println("Entro");
    while(i<100){ //falla en la comprobacion no entra en el bucle
        println(i);
        i=i+1;
    }
    println("Salgo");
}